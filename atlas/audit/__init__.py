"""
ATLAS Historical Readiness Audit toolkit.

This package contains **read-only** inspection utilities used by the
``atlas-historical-readiness-audit`` GitHub Actions workflow. Nothing in
this package deletes, overwrites, renames, moves, or uploads any Cloud
Storage object, and nothing here runs the 2024 rebuild or the 2025
backtest. The audit only inspects the repository and reports on already
published/known data -- it never invents files, schemas, modules, or
pipeline stages that do not exist.
"""
