{
  "contract_name": "frozen_scoring_bucket_contract",
  "contract_version": "2024_v1",
  "created_at_utc": "2026-07-20T22:46:00Z",
  "status": "frozen",
  "purpose": "Freeze the total-run bucket boundaries used by atlas.learning.totals_target_builder so they are computed exactly once, from the 2024 discovery season only, and are never recomputed on later runs or influenced by 2025+ outcomes.",
  "discovery_season": 2024,
  "source_dataset": {
    "path": "data/game_intelligence/factual_learning_targets/2024/factual_game_learning_targets.parquet",
    "sample_reference": "atlas_reference/samples/games/data__game_intelligence__factual_learning_targets__2024__factual_game_learning_targets.parquet.games.parquet",
    "column_used": "game_total_runs",
    "source_dataset_sha256": "2891426f18491cac56c2d7d3d30193f7b1f181eb6eaae0075074b62eaa64e50f",
    "sha256_scope": "atlas_reference sample file bytes (games slice); authoritative full-population hash must be recorded here when the full 2024 dataset is rehashed under atlas_reference/manifest.json's checksum policy."
  },
  "sample_size": 2428,
  "percentile_method": "linear interpolation (pandas default, equivalent to numpy 'linear' method) over the completed-game 2024 game_total_runs distribution",
  "percentile_values": {
    "p25": 5,
    "p50": 8,
    "p75": 11,
    "p90": 15
  },
  "bucket_boundaries": {
    "low_scoring_max_runs": 5,
    "high_scoring_min_runs": 12,
    "extreme_high_scoring_min_runs": 15
  },
  "boundary_derivation_notes": [
    "low_scoring_max_runs is the p25 (bottom-quartile) cut point, taken as-is.",
    "high_scoring_min_runs is p75 + 1 (11 + 1 = 12), i.e. one run above the 75th percentile, so it isolates the top quartile strictly above the median band.",
    "extreme_high_scoring_min_runs is the p90 cut point, taken as-is."
  ],
  "immutability_rules": [
    "These boundaries are discovered once from the 2024 completed-game distribution and then frozen.",
    "atlas.learning.totals_target_builder must never recompute percentiles from its runtime inputs.",
    "The frozen 2024 boundaries are reused unchanged for 2025+ blind validation.",
    "No 2025 or later outcome may be used to compute, adjust, or re-derive these boundaries.",
    "Changing these values requires a new contract_version, not an in-place edit of this file."
  ],
  "validation_isolation": {
    "season_used_for_discovery": 2024,
    "2025_outcomes_used": false,
    "2026_outcomes_used": false,
    "recomputed_at_runtime": false
  }
}
